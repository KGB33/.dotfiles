(include "utils.yuck")

(defpoll dockerActive :interval "60s" "docker ps |& rg Cannot")
(defwidget docker-icon []
  (icon-module 
    ; TODO: make it so dockerActive isn't optimized away
    ; and can be used for :visible
    :icon "${"" == dockerActive ? "" : ""}"
    :class "docker-icon"
    :halign "end"
    :spacing 0
    ;:visible "${"" == dockerActive}"
  )
)
